from setuptools import find_packages, setup

setup(
    name='pyBlindDriver',
    packages=find_packages(include=['pyBlindDriver']),
    version='0.0.1',
    description='My first Python library',
    author='0noder4',
    license='MIT',
)